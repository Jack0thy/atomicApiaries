/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/atomic-apiaries', // Replace with your repository name
}

module.exports = nextConfig 